# Robust Annotator (Streamlit)
A simple app to filter/accept dataset pairs (text + hypo/adversarial images).

Data source: Google Drive (via service account).
Outputs: filtered JSONL per category + copies of accepted images in filtered folders.